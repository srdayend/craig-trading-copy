# Outputs

Only curated text outputs are committed here:

- Markdown reports;
- rule/config YAML files;
- small JSON summaries that do not contain source media URLs.

Large generated CSV, Parquet, XLSX, NDJSON, image, archive, and log files are ignored. Those files remain local research artifacts.
